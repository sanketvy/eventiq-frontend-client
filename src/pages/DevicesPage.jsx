const DevicesPage = ({selectedProject, selectedTimeRange}) => {

    return (
        <div>
            <h1>Devices {selectedProject} - {selectedTimeRange}</h1>
        </div>
    )
}

export default DevicesPage;